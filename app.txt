const STORAGE_KEYS={pacientes:'ume_simple_pacientes_plus',procedimentos:'ume_simple_procedimentos_plus',agendamentos:'ume_simple_agendamentos_plus',fichas:'ume_clinica_fichas_print',screen:'ume_simple_active_screen',installHintDismissed:'ume_install_hint_dismissed'};
let pacientes=JSON.parse(localStorage.getItem(STORAGE_KEYS.pacientes)||'[]');
let procedimentos=JSON.parse(localStorage.getItem(STORAGE_KEYS.procedimentos)||'[]');
let agendamentos=JSON.parse(localStorage.getItem(STORAGE_KEYS.agendamentos)||'[]');
let fichas=JSON.parse(localStorage.getItem(STORAGE_KEYS.fichas)||'[]');
let pacienteFotoTemp='';let arquivosPacienteTemp=[];let antesDepoisTemp=[];let pacienteEditandoId=null;let beforeAfterFotoTemp=null;let agendamentoEditandoId=null;
const $=(id)=>document.getElementById(id);
const esc=(v)=>String(v??'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/\"/g,'&quot;').replace(/'/g,'&#039;');
const todayISO=()=>{const d=new Date();return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`};
const tomorrowISO=()=>{const d=new Date();d.setDate(d.getDate()+1);return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`};
const brDate=(iso)=>iso?`${iso.slice(8,10)}/${iso.slice(5,7)}/${iso.slice(0,4)}`:'';
const bytes=(v)=>v<1024?`${v} B`:v<1048576?`${(v/1024).toFixed(1)} KB`:`${(v/1048576).toFixed(1)} MB`;
function save(){localStorage.setItem(STORAGE_KEYS.pacientes,JSON.stringify(pacientes));localStorage.setItem(STORAGE_KEYS.procedimentos,JSON.stringify(procedimentos));localStorage.setItem(STORAGE_KEYS.agendamentos,JSON.stringify(agendamentos));localStorage.setItem(STORAGE_KEYS.fichas,JSON.stringify(fichas));}
function toast(msg){const t=$('toast');t.textContent=msg;t.classList.remove('hidden');t.classList.add('show');clearTimeout(window.__toastTimer);window.__toastTimer=setTimeout(()=>{t.classList.remove('show');t.classList.add('hidden')},3000)}
function setAppHeight(){document.documentElement.style.setProperty('--app-vh',`${window.innerHeight*0.01}px`)}
function closeAllModals(){document.querySelectorAll('.modal').forEach(m=>m.classList.add('hidden'));document.body.classList.remove('modal-open')}
function openModal(id){$(id)?.classList.remove('hidden');document.body.classList.add('modal-open')}
function closeModal(id){$(id)?.classList.add('hidden');const anyOpen=[...document.querySelectorAll('.modal')].some(m=>!m.classList.contains('hidden'));document.body.classList.toggle('modal-open',anyOpen)}
function showScreen(id){['screen-home','screen-agenda','screen-pacientes','screen-fichas'].forEach(screenId=>{const el=$(screenId);el.classList.remove('active');el.setAttribute('aria-hidden','true')});requestAnimationFrame(()=>{const target=$(id);target.classList.add('active');target.setAttribute('aria-hidden','false')});localStorage.setItem(STORAGE_KEYS.screen,id);window.scrollTo({top:0,behavior:'smooth'})}
function syncDesktopNav(activeId){document.querySelectorAll('.desktop-nav-btn').forEach(btn=>btn.classList.toggle('active',btn.dataset.goScreen===activeId))}
function showHome(){closeAllModals();showScreen('screen-home');syncDesktopNav('screen-home')} function showAgenda(){renderAgenda();showScreen('screen-agenda');syncDesktopNav('screen-agenda')} function showPacientes(focus=false){renderPacientes();showScreen('screen-pacientes');syncDesktopNav('screen-pacientes');if(focus)setTimeout(()=>$('buscaPaciente').focus(),220)} function showFichas(){renderFichas();showScreen('screen-fichas');syncDesktopNav('screen-fichas')}
function uniqueAgendamentos(list){const map=new Map();list.forEach(a=>{const key=[a.pacienteId||a.pacienteNome||'',a.procedimentoId||a.procedimentoNome||'',a.data||'',a.hora||''].join('|');if(!map.has(key))map.set(key,a)});return [...map.values()]}
function comparisonsFromList(list){const sorted=[...list].sort((a,b)=>(a.data||'').localeCompare(b.data||''));const befores=sorted.filter(x=>x.tipo==='Antes');const afters=sorted.filter(x=>x.tipo==='Depois');const pairs=[];const maxLen=Math.max(befores.length,afters.length);for(let i=0;i<maxLen;i++){if(befores[i]||afters[i])pairs.push({antes:befores[i]||null,depois:afters[i]||null,indice:i+1})}return pairs}
function comparisonCard(pair){const antesImg=pair.antes?.foto||'';const depoisImg=pair.depois?.foto||'';const antesData=pair.antes?.data?brDate(pair.antes.data):'Sem foto';const depoisData=pair.depois?.data?brDate(pair.depois.data):'Sem foto';const avaliacao=pair.depois?.avaliacao||pair.antes?.avaliacao||'Sem avaliação';return `<article class="comparativo-card"><div class="comparativo-head"><strong>Comparativo ${pair.indice}</strong><span class="badge">Antes × Depois</span></div><div class="comparativo-miniaturas"><div class="comparativo-thumb">${antesImg?`<img src="${antesImg}" alt="Antes">`:''}<span class="comparativo-tag">Antes • ${esc(antesData)}</span></div><div class="comparativo-thumb">${depoisImg?`<img src="${depoisImg}" alt="Depois">`:''}<span class="comparativo-tag">Depois • ${esc(depoisData)}</span></div></div><div class="comparativo-legenda"><span><strong>Avaliação:</strong> ${esc(avaliacao)}</span></div></article>`}
function updatePacienteHistoryButton(){ $('btnVerAgendamentosPacienteModal')?.classList.toggle('hidden',!pacienteEditandoId)}
function fillAgendamentoSelects(){$('agendamentoPaciente').innerHTML=pacientes.length?pacientes.map(p=>`<option value="${p.id}">${esc(p.nome)}</option>`).join(''):'<option value="">Cadastre um paciente primeiro</option>';$('agendamentoProcedimento').innerHTML=procedimentos.length?procedimentos.map(p=>`<option value="${p.id}">${esc(p.nome)}</option>`).join(''):'<option value="">Cadastre um procedimento primeiro</option>'}
function setRecorrenciaDisabled(disabled){$('recorrenciaTipo').disabled=disabled;$('recorrenciaQuantidade').disabled=disabled}
function clearAgendamentoModal(){agendamentoEditandoId=null;fillAgendamentoSelects();$('agendamentoData').value=todayISO();$('agendamentoHora').value='';$('agendamentoProfissional').value='';$('agendamentoDescricaoCurta').value='';$('agendamentoStatus').value='agendado';$('recorrenciaTipo').value='nenhuma';$('recorrenciaQuantidade').value=1;setRecorrenciaDisabled(false);$('tituloModalAgendamento').textContent='Novo agendamento';$('salvarAgendamento').textContent='Salvar agendamento';$('btnExcluirAgendamentoModal').classList.add('hidden')}
function recurrenceDates(base,tipo,qtd){const total=Math.max(1,Number(qtd)||1);if(tipo==='nenhuma'||total===1)return[base];const list=[];for(let i=0;i<total;i++){const d=new Date(`${base}T12:00:00`);if(tipo==='semanal')d.setDate(d.getDate()+7*i);if(tipo==='quinzenal')d.setDate(d.getDate()+14*i);if(tipo==='mensal')d.setMonth(d.getMonth()+i);list.push(`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`)}return list}
function hasConflict(data,hora,ignoreId=null){return agendamentos.some(a=>a.id!==ignoreId&&a.data===data&&a.hora===hora&&a.status!=='cancelou'&&a.status!=='remarcou')}
function statusClass(s){const st=(s||'agendado').toLowerCase();return['agendado','compareceu','faltou','cancelou','remarcou'].includes(st)?st:'agendado'}
window.atualizarStatusAgendamento=(id,status)=>{const idx=agendamentos.findIndex(a=>a.id===id);if(idx<0)return;agendamentos[idx].status=status;save();renderAgenda();toast(`Status atualizado para ${status}.`)};
function findPaciente(id){return pacientes.find(p=>p.id===id)}
function normalizedPhone(v){const d=String(v||'').replace(/\D/g,'');if(!d)return'';return(d.length===10||d.length===11)?`55${d}`:d}
function reminderText(a){return `Olá, ${a.pacienteNome}! Passando para lembrar do seu agendamento na UME Estética Avançada.\n\n📅 Data: ${brDate(a.data)}\n⏰ Horário: ${String(a.hora||'').slice(0,5)}\n💆 Procedimento: ${a.procedimentoNome}\n👩‍⚕️ Profissional: ${a.profissional}\n\nSe precisar remarcar, me avise por aqui.`}
function waLink(a){const tel=normalizedPhone(a.pacienteTelefone||findPaciente(a.pacienteId)?.telefone||'');return tel?`https://wa.me/${tel}?text=${encodeURIComponent(reminderText(a))}`:null}
function sent(a){return!!a.lembreteWhatsAppEnviadoEm}
function markSent(id){const idx=agendamentos.findIndex(a=>a.id===id);if(idx>=0){agendamentos[idx].lembreteWhatsAppEnviadoEm=new Date().toISOString();save()}}
window.enviarWhatsAppAgendamento=(id)=>{const a=agendamentos.find(x=>x.id===id);if(!a)return;const link=waLink(a);if(!link)return toast('Cadastre o telefone do paciente para usar o WhatsApp.');window.open(link,'_blank');markSent(id);renderAgenda();toast('Lembrete marcado como enviado.')};
function amanhaList(){return uniqueAgendamentos(agendamentos.filter(a=>a.data===tomorrowISO()&&a.status!=='cancelou'&&a.status!=='remarcou')).sort((a,b)=>`${a.data} ${a.hora}`.localeCompare(`${b.data} ${b.hora}`))}
window.abrirLembretesAmanha=()=>{const list=amanhaList().filter(a=>waLink(a)&&!sent(a));if(!list.length)return toast('Não há lembretes pendentes válidos para amanhã.');let opened=0;list.forEach((a,i)=>setTimeout(()=>{const win=window.open(waLink(a),'_blank');if(win){opened++;markSent(a.id)}if(i===list.length-1)setTimeout(()=>{renderAgenda();toast(opened<list.length?'Alguns lembretes podem ter sido bloqueados pelo navegador.':`Foram abertos ${opened} lembrete(s) de amanhã.`)},250)},i*350))}
function renderAmanha(){const list=amanhaList();if(!list.length){$('agendamentosAmanhaLista').innerHTML='<div class="empty-box">Nenhum agendamento para amanhã.</div>';return}$('agendamentosAmanhaLista').innerHTML=list.map(a=>{let action='<button class="btn ghost small" disabled>Sem WhatsApp</button>';if(waLink(a)&&sent(a))action='<span class="badge">Lembrete enviado</span>';else if(waLink(a))action=`<button class="btn whatsapp small" type="button" onclick="enviarWhatsAppAgendamento(${a.id})">Enviar lembrete</button>`;return `<article class="panel glass"><div class="panel-head"><div><h4>${esc(a.pacienteNome)}</h4><small class="muted">${brDate(a.data)} às ${esc(String(a.hora||'').slice(0,5))}</small></div><span class="status-pill ${statusClass(a.status)}">${esc(a.status||'agendado')}</span></div><div class="ficha-meta"><strong>Procedimento:</strong> ${esc(a.procedimentoNome)}</div><div class="ficha-meta"><strong>Profissional:</strong> ${esc(a.profissional)}</div><div class="ficha-meta"><strong>Telefone:</strong> ${esc(a.pacienteTelefone||findPaciente(a.pacienteId)?.telefone||'-')}</div><div class="ficha-actions">${action}</div></article>`}).join('')}
function nextAppointment(){const nowKey=`${todayISO()} ${new Date().toTimeString().slice(0,5)}`;return uniqueAgendamentos([...agendamentos]).sort((a,b)=>`${a.data} ${a.hora}`.localeCompare(`${b.data} ${b.hora}`)).find(a=>`${a.data} ${a.hora}`>=nowKey&&a.status!=='cancelou'&&a.status!=='remarcou')||null}
window.editarAgendamento=(id)=>{const a=agendamentos.find(x=>x.id===id);if(!a)return;agendamentoEditandoId=id;fillAgendamentoSelects();$('agendamentoPaciente').value=String(a.pacienteId||'');$('agendamentoProcedimento').value=String(a.procedimentoId||'');$('agendamentoData').value=a.data||todayISO();$('agendamentoHora').value=a.hora||'';$('agendamentoProfissional').value=a.profissional||'';$('agendamentoDescricaoCurta').value=a.descricaoCurta||'';$('agendamentoStatus').value=a.status||'agendado';$('recorrenciaTipo').value=a.recorrencia||'nenhuma';$('recorrenciaQuantidade').value=a.totalPacote||1;setRecorrenciaDisabled(true);$('tituloModalAgendamento').textContent='Editar agendamento';$('salvarAgendamento').textContent='Atualizar agendamento';$('btnExcluirAgendamentoModal').classList.remove('hidden');openModal('modalAgendamento')};
window.excluirAgendamento=(id)=>{const a=agendamentos.find(x=>x.id===id);if(!a)return;let removerTudoDoPacote=false;if(a.pacoteId){removerTudoDoPacote=confirm('Este agendamento faz parte de uma recorrência/pacote.\n\nClique em OK para excluir TODAS as sessões desse pacote.\nClique em Cancelar para excluir somente esta sessão.')}const confirmar=confirm(removerTudoDoPacote&&a.pacoteId?`Deseja realmente excluir todas as sessões do pacote de ${a.pacienteNome}?`:`Deseja realmente excluir o agendamento de ${a.pacienteNome} em ${brDate(a.data)} às ${String(a.hora||'').slice(0,5)}?`);if(!confirmar)return;if(removerTudoDoPacote&&a.pacoteId)agendamentos=agendamentos.filter(x=>x.pacoteId!==a.pacoteId);else agendamentos=agendamentos.filter(x=>x.id!==id);save();renderAgenda();closeModal('modalAgendamento');clearAgendamentoModal();toast('Agendamento removido com sucesso.')};
window.excluirPaciente=(id)=>{const p=pacientes.find(x=>x.id===id);if(!p)return;const qtdAg=agendamentos.filter(a=>a.pacienteId===id).length;const msg=qtdAg?`Deseja excluir o paciente \"${p.nome}\"?\n\nIsso também removerá ${qtdAg} agendamento(s) vinculado(s).`:`Deseja excluir o paciente \"${p.nome}\"?`;if(!confirm(msg))return;pacientes=pacientes.filter(x=>x.id!==id);agendamentos=agendamentos.filter(a=>a.pacienteId!==id);save();fillAgendamentoSelects();renderPacientes();renderAgenda();closeModal('modalPaciente');clearPacienteModal();toast('Paciente removido com sucesso.')};
function renderAgenda(){const fData=$('filtroDataAgenda').value;const busca=$('filtroBuscaAgenda').value.toLowerCase().trim();const hoje=todayISO();const list=uniqueAgendamentos(agendamentos.filter(a=>(!fData||a.data===fData)&&(!busca||String(a.pacienteNome||'').toLowerCase().includes(busca)||String(a.procedimentoNome||'').toLowerCase().includes(busca)))).sort((a,b)=>`${a.data} ${a.hora}`.localeCompare(`${b.data} ${b.hora}`));$('summaryTotalHoje').textContent=uniqueAgendamentos(agendamentos.filter(a=>a.data===hoje&&a.status!=='cancelou'&&a.status!=='remarcou')).length;$('totalProximos').textContent=uniqueAgendamentos(agendamentos.filter(a=>a.data>=hoje&&a.status!=='cancelou')).length;$('totalAmanha').textContent=amanhaList().length;$('agendaDiaTitulo').textContent=fData?(fData===hoje?'Hoje':brDate(fData)):'Agenda completa';$('agendaDiaTotal').textContent=list.length;renderAmanha();const next=nextAppointment();$('proximoAtendimentoPaciente').textContent=next?next.pacienteNome:'Nenhum atendimento';$('proximoAtendimentoResumo').textContent=next?`${brDate(next.data)} às ${String(next.hora||'').slice(0,5)} • ${next.procedimentoNome}`:'Adicione agendamentos para visualizar o próximo horário.';if(!list.length){$('agendaLista').innerHTML='<div class="agenda-empty">Nenhum agendamento encontrado.</div>';return}$('agendaLista').innerHTML=list.map(a=>`<article class="agenda-row" role="button" tabindex="0" onclick="editarAgendamento(${a.id})" onkeydown="if(event.key==='Enter'||event.key===' '){event.preventDefault();editarAgendamento(${a.id})}"><span class="agenda-cell" data-label="Data">${brDate(a.data)}</span><span class="agenda-cell" data-label="Hora">${esc(String(a.hora||'').slice(0,5))}</span><span class="agenda-cell patient-cell" data-label="Paciente">${esc(a.pacienteNome)}</span><span class="agenda-cell procedure-cell" data-label="Procedimento">${esc(a.procedimentoNome)}</span></article>`).join('')}
function renderArquivosPacienteTemp(){const box=$('listaArquivosPaciente');if(!arquivosPacienteTemp.length){box.innerHTML='<div class="empty-box">Nenhum arquivo anexado.</div>';return}box.innerHTML=arquivosPacienteTemp.map((a,i)=>`<div class="file-item"><div><strong>${esc(a.nome)}</strong><small>${bytes(a.tamanho)}</small></div><button class="btn ghost small" type="button" onclick="removerArquivoTemp(${i})">Remover</button></div>`).join('')}
window.removerArquivoTemp=(i)=>{arquivosPacienteTemp.splice(i,1);renderArquivosPacienteTemp()};
function renderAntesDepoisTemp(){const box=$('listaAntesDepoisPaciente');const comparativos=$('comparativosAntesDepoisTemp');if(!antesDepoisTemp.length){box.innerHTML='<div class="empty-box">Nenhuma foto de antes e depois cadastrada ainda.</div>';comparativos.innerHTML='<div class="empty-box">Adicione fotos para gerar miniaturas comparativas.</div>';return}box.innerHTML=antesDepoisTemp.map((item,i)=>`<article class="panel glass before-card"><img src="${item.foto}" alt="${esc(item.tipo)}"><div class="before-meta"><span class="badge">${esc(item.tipo)}</span><div class="ficha-meta"><strong>Data:</strong> ${brDate(item.data)}</div><div class="ficha-meta"><strong>Avaliação:</strong> ${esc(item.avaliacao||'Sem avaliação')}</div><div class="section-actions"><button class="btn ghost small" type="button" onclick="removerAntesDepoisTemp(${i})">Remover</button></div></div></article>`).join('');const pairs=comparisonsFromList(antesDepoisTemp);comparativos.innerHTML=pairs.length?pairs.map(comparisonCard).join(''):'<div class="empty-box">Adicione pelo menos um Antes e um Depois para comparar.</div>'}
window.removerAntesDepoisTemp=(i)=>{antesDepoisTemp.splice(i,1);renderAntesDepoisTemp()};
function updatePhoto(){const img=$('pacienteFotoPreview');const placeholder=$('pacienteFotoPlaceholder');if(pacienteFotoTemp){img.src=pacienteFotoTemp;img.style.display='block';placeholder.style.display='none'}else{img.src='';img.style.display='none';placeholder.style.display='grid'}}
function clearPacienteModal(){pacienteEditandoId=null;pacienteFotoTemp='';arquivosPacienteTemp=[];antesDepoisTemp=[];beforeAfterFotoTemp=null;['pacienteNome','pacienteTelefone','pacienteCpf','pacienteEmail','pacienteNascimento','pacienteEndereco','pacienteObservacoes','beforeAfterAvaliacao'].forEach(id=>$(id).value='');$('beforeAfterTipo').value='Antes';$('beforeAfterData').value=todayISO();$('beforeAfterFotoInput').value='';$('tituloModalPaciente').textContent='Novo paciente';$('btnExcluirPacienteModal').classList.add('hidden');updatePacienteHistoryButton();updatePhoto();renderArquivosPacienteTemp();renderAntesDepoisTemp()}
function renderPacientes(){const termo=$('buscaPaciente').value.toLowerCase().trim();const num=termo.replace(/\D/g,'');const list=pacientes.filter(p=>{const cpf=String(p.cpf||'').replace(/\D/g,'');const tel=String(p.telefone||'').replace(/\D/g,'');return !termo||String(p.nome||'').toLowerCase().includes(termo)||(num&&cpf.includes(num))||(num&&tel.includes(num))});if(!list.length){$('pacientesLista').innerHTML='<div class="empty-box">Nenhum paciente encontrado.</div>';return}$('pacientesLista').innerHTML=list.map(p=>`<article class="patient-card glass"><div class="card-top">${p.foto?`<img src="${p.foto}" class="patient-card-photo" alt="${esc(p.nome)}">`:`<div class="patient-card-photo-placeholder">${esc((p.nome||'P').charAt(0).toUpperCase())}</div>`}<div><h3>${esc(p.nome)}</h3><p><strong>Telefone:</strong> ${esc(p.telefone||'-')}</p><p><strong>CPF:</strong> ${esc(p.cpf||'-')}</p><p><strong>E-mail:</strong> ${esc(p.email||'-')}</p></div></div><div class="patient-card-stats"><span>Arquivos anexados: ${(p.arquivos||[]).length}</span><span>Antes e Depois: ${(p.antesDepois||[]).length} foto(s)</span><small>${esc(p.observacoes||'Sem observações cadastradas.')}</small></div><div class="patient-card-actions"><button class="btn secondary small" type="button" onclick="editarPaciente(${p.id})">Editar</button><button class="btn ghost small" type="button" onclick="visualizarAgendamentosPaciente(${p.id})">Agendamentos</button><button class="btn ghost small" type="button" onclick="visualizarArquivosPaciente(${p.id})">Arquivos</button><button class="btn ghost small" type="button" onclick="visualizarAntesDepoisPaciente(${p.id})">Antes e Depois</button><button class="btn ghost small" type="button" onclick="excluirPaciente(${p.id})">Excluir</button></div></article>`).join('')}
window.editarPaciente=(id)=>{const p=pacientes.find(x=>x.id===id);if(!p)return;pacienteEditandoId=id;$('pacienteNome').value=p.nome||'';$('pacienteTelefone').value=p.telefone||'';$('pacienteCpf').value=p.cpf||'';$('pacienteEmail').value=p.email||'';$('pacienteNascimento').value=p.nascimento||'';$('pacienteEndereco').value=p.endereco||'';$('pacienteObservacoes').value=p.observacoes||'';pacienteFotoTemp=p.foto||'';arquivosPacienteTemp=[...(p.arquivos||[])];antesDepoisTemp=[...(p.antesDepois||[])];$('beforeAfterData').value=todayISO();$('tituloModalPaciente').textContent='Editar paciente';$('btnExcluirPacienteModal').classList.remove('hidden');updatePacienteHistoryButton();updatePhoto();renderArquivosPacienteTemp();renderAntesDepoisTemp();openModal('modalPaciente')};
window.visualizarAgendamentosPaciente=(id)=>{const p=pacientes.find(x=>x.id===id);if(!p)return;const list=uniqueAgendamentos([...agendamentos].filter(a=>a.pacienteId===id||a.pacienteNome===p.nome)).sort((a,b)=>`${b.data} ${b.hora}`.localeCompare(`${a.data} ${a.hora}`));$('tituloModalAgendamentosPaciente').textContent=`Agendamentos de ${p.nome}`;const nowTime=new Date().toTimeString().slice(0,5);const realizados=list.filter(a=>a.data<todayISO()||(a.data===todayISO()&&String(a.hora||'')<nowTime)).length;const futuros=list.length-realizados;$('resumoAgendamentosPaciente').innerHTML=`<div class="history-stat"><strong>${list.length}</strong><small>Total</small></div><div class="history-stat"><strong>${realizados}</strong><small>Histórico</small></div><div class="history-stat"><strong>${futuros}</strong><small>Futuros</small></div>`;$('listaAgendamentosPaciente').innerHTML=!list.length?'<div class="empty-box">Nenhum agendamento encontrado para este paciente.</div>':list.map(a=>`<article class="patient-schedule-item ${a.data<todayISO()?'past':'future'}"><div class="patient-schedule-main"><strong>${brDate(a.data)} • ${esc(String(a.hora||'').slice(0,5))}</strong><span>${esc(a.procedimentoNome)}</span><small>${esc(a.profissional||'Profissional não informado')}</small></div><span class="status-pill ${statusClass(a.status)}">${esc(a.status||'agendado')}</span></article>`).join('');openModal('modalAgendamentosPaciente')};
window.visualizarArquivosPaciente=(id)=>{const p=pacientes.find(x=>x.id===id);if(!p)return;const files=p.arquivos||[];$('visualizadorArquivosPaciente').innerHTML=!files.length?'<div class="empty-box">Nenhum arquivo anexado para este paciente.</div>':files.map(a=>`<div class="file-item"><div><strong>${esc(a.nome)}</strong><small>${bytes(a.tamanho)}</small></div><a class="btn ghost small" href="${a.conteudo}" download="${esc(a.nome)}">Baixar</a></div>`).join('');openModal('modalArquivosPaciente')};
window.visualizarAntesDepoisPaciente=(id)=>{const p=pacientes.find(x=>x.id===id);if(!p)return;const list=p.antesDepois||[];$('visualizadorComparativosPaciente').innerHTML=comparisonsFromList(list).length?comparisonsFromList(list).map(comparisonCard).join(''):'<div class="empty-box">Adicione pelo menos um Antes e um Depois para ter miniaturas comparativas.</div>';$('visualizadorAntesDepoisPaciente').innerHTML=!list.length?'<div class="empty-box">Nenhuma foto de antes e depois cadastrada.</div>':list.map(a=>`<article class="panel glass before-card"><img src="${a.foto}" alt="${esc(a.tipo)}"><div class="before-meta"><span class="badge">${esc(a.tipo)}</span><div class="ficha-meta"><strong>Data:</strong> ${brDate(a.data)}</div><div class="ficha-meta"><strong>Avaliação:</strong> ${esc(a.avaliacao||'Sem avaliação')}</div></div></article>`).join('');openModal('modalAntesDepoisPaciente')};
function classifyFicha(fileName){const name=fileName.toLowerCase();if(name.includes('termo'))return'Termo';if(name.includes('ficha'))return'Ficha';if(name.includes('form'))return'Formulário';return'Outros'}
async function processFichaFiles(files){const maxBytes=4.5*1024*1024;for(const f of files){if(f.size>maxBytes){toast(`O arquivo ${f.name} é muito grande para salvar localmente.`);continue}const dataUrl=await new Promise(ok=>{const r=new FileReader();r.onload=x=>ok(x.target.result);r.readAsDataURL(f)});fichas.unshift({id:Date.now()+Math.floor(Math.random()*1000),nome:f.name,categoria:classifyFicha(f.name),tipo:f.type||'application/octet-stream',tamanho:f.size,conteudo:dataUrl,criadoEm:new Date().toISOString()})}save();renderFichas();toast('Arquivo(s) adicionado(s) à biblioteca de fichas.')}
function openFicha(id){const ficha=fichas.find(f=>f.id===id);if(!ficha)return;const win=window.open('','_blank');if(!win)return toast('O navegador bloqueou a abertura da ficha.');const isPdf=ficha.tipo.includes('pdf');const isImage=ficha.tipo.startsWith('image/');if(isPdf){win.document.write(`<title>${esc(ficha.nome)}</title><style>html,body,iframe{margin:0;height:100%;width:100%}</style><iframe src="${ficha.conteudo}"></iframe>`);win.document.close();return}if(isImage){win.document.write(`<title>${esc(ficha.nome)}</title><style>body{margin:0;display:grid;place-items:center;background:#181513}img{max-width:100%;max-height:100vh}</style><img src="${ficha.conteudo}" alt="${esc(ficha.nome)}">`);win.document.close();return}win.location.href=ficha.conteudo}
function imprimirFicha(id){const ficha=fichas.find(f=>f.id===id);if(!ficha)return;const isPdf=ficha.tipo.includes('pdf');const isImage=ficha.tipo.startsWith('image/');if(!isPdf&&!isImage){toast('Este tipo de arquivo não é impresso direto no navegador. Use Baixar e imprima no computador.');return}const win=window.open('','_blank');if(!win)return toast('O navegador bloqueou a impressão.');if(isPdf){win.document.write(`<title>${esc(ficha.nome)}</title><style>html,body,iframe{margin:0;height:100%;width:100%}</style><iframe src="${ficha.conteudo}"></iframe><script>setTimeout(() => window.print(), 900)<\\/script>`)}else{win.document.write(`<title>${esc(ficha.nome)}</title><style>body{margin:0;display:grid;place-items:center;background:#fff}img{max-width:100%;max-height:100vh}</style><img src="${ficha.conteudo}" alt="${esc(ficha.nome)}"><script>window.onload = () => setTimeout(() => window.print(), 250)<\\/script>`)}win.document.close()}
window.abrirFicha=openFicha;window.imprimirFicha=imprimirFicha;window.removerFicha=(id)=>{fichas=fichas.filter(f=>f.id!==id);save();renderFichas();toast('Ficha removida.')};
function renderFichas(){const busca=$('filtroBuscaFicha').value.toLowerCase().trim();const categoria=$('filtroCategoriaFicha').value;const list=fichas.filter(f=>(!busca||f.nome.toLowerCase().includes(busca))&&(!categoria||f.categoria===categoria));if(!list.length){$('fichasLista').innerHTML='<div class="empty-box">Nenhuma ficha cadastrada ainda. Use “Adicionar arquivos” para montar a biblioteca da clínica.</div>';return}$('fichasLista').innerHTML=list.map(f=>`<article class="ficha-card glass"><div class="ficha-title"><div><span class="ficha-category">${esc(f.categoria)}</span><h3>${esc(f.nome)}</h3></div><small class="ficha-meta">${bytes(f.tamanho)} • ${new Date(f.criadoEm).toLocaleDateString('pt-BR')}</small></div><div class="ficha-meta"><strong>Tipo:</strong> ${esc(f.tipo||'arquivo')}</div><div class="ficha-actions"><button class="btn secondary small" type="button" onclick="abrirFicha(${f.id})">Abrir</button><a class="btn ghost small" download="${esc(f.nome)}" href="${f.conteudo}">Baixar</a><button class="btn primary small" type="button" onclick="imprimirFicha(${f.id})">Imprimir</button><button class="btn ghost small" type="button" onclick="removerFicha(${f.id})">Remover</button></div></article>`).join('')}
function exportBackup(){const backup={app:'UME Estética Avançada',versao:'agenda-desktop-pro',exportadoEm:new Date().toISOString(),pacientes,procedimentos,agendamentos,fichas};const blob=new Blob([JSON.stringify(backup,null,2)],{type:'application/json;charset=utf-8'});const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download=`ume_backup_${todayISO()}.json`;document.body.appendChild(a);a.click();a.remove();URL.revokeObjectURL(url);toast('Backup exportado com sucesso.')}
async function restoreBackup(file){if(!file)return;try{const data=JSON.parse(await file.text());if(!Array.isArray(data.pacientes)||!Array.isArray(data.procedimentos)||!Array.isArray(data.agendamentos))throw new Error();pacientes=data.pacientes;procedimentos=data.procedimentos;agendamentos=data.agendamentos;fichas=Array.isArray(data.fichas)?data.fichas:[];save();fillAgendamentoSelects();renderAgenda();renderPacientes();renderFichas();toast('Backup restaurado com sucesso.')}catch{toast('Não foi possível restaurar o backup.')}}
function seed(){if(!pacientes.length&&!procedimentos.length&&!agendamentos.length){const p={id:1,nome:'Maria Silva',telefone:'(41) 99999-1111',cpf:'123.456.789-00',email:'maria@email.com',nascimento:'1990-05-10',endereco:'Rua das Flores, 123',observacoes:'Paciente recorrente',foto:'',arquivos:[],antesDepois:[]};const pr={id:1,nome:'Limpeza de Pele',descricao:'Higienização e extração leve.',valor:180};pacientes=[p];procedimentos=[pr];agendamentos=[{id:1,pacienteId:1,pacienteNome:p.nome,pacienteTelefone:p.telefone,procedimentoId:1,procedimentoNome:pr.nome,procedimentoDescricao:pr.descricao,procedimentoValor:pr.valor,data:todayISO(),hora:'14:00',profissional:'Dra. Ana',descricaoCurta:'Sessão facial rápida',recorrencia:'nenhuma',totalPacote:1,ordemPacote:1,pacoteId:null,status:'agendado',lembreteWhatsAppEnviadoEm:null}];save()}}
function bind(){document.querySelectorAll('[data-close]').forEach(b=>b.addEventListener('click',()=>closeModal(b.dataset.close)));document.querySelectorAll('[data-go-home]').forEach(b=>b.addEventListener('click',showHome));document.querySelectorAll('[data-go-screen]').forEach(b=>b.addEventListener('click',()=>{const id=b.dataset.goScreen;if(id==='screen-home')showHome();else if(id==='screen-agenda')showAgenda();else if(id==='screen-pacientes')showPacientes();else if(id==='screen-fichas')showFichas()}));document.querySelectorAll('.modal').forEach(m=>m.addEventListener('click',e=>{if(e.target===m)closeModal(m.id)}));document.addEventListener('keydown',e=>{if(e.key==='Escape'){const open=[...document.querySelectorAll('.modal')].find(m=>!m.classList.contains('hidden'));if(open)closeModal(open.id)}});$('btnHomeAgendamento').addEventListener('click',()=>{clearAgendamentoModal();openModal('modalAgendamento')});$('btnHomePaciente').addEventListener('click',()=>{clearPacienteModal();openModal('modalPaciente')});$('btnHomeProcedimento').addEventListener('click',()=>openModal('modalProcedimento'));$('btnHomePesquisarPaciente').addEventListener('click',()=>showPacientes(true));$('btnHomeVerAgenda').addEventListener('click',showAgenda);$('btnHomeFichas').addEventListener('click',showFichas);$('desktopQuickAgendamento')?.addEventListener('click',()=>{clearAgendamentoModal();openModal('modalAgendamento')});$('desktopQuickPaciente')?.addEventListener('click',()=>{clearPacienteModal();openModal('modalPaciente')});$('btnNovoAgendamentoTela').addEventListener('click',()=>{clearAgendamentoModal();openModal('modalAgendamento')});$('btnNovoPacienteTela').addEventListener('click',()=>{clearPacienteModal();openModal('modalPaciente')});$('btnAgendaDoDia').addEventListener('click',()=>{$('filtroDataAgenda').value=todayISO();$('filtroBuscaAgenda').value='';renderAgenda();toast('Agenda do dia carregada.')});$('btnTodosAgendamentos').addEventListener('click',()=>{$('filtroDataAgenda').value='';$('filtroBuscaAgenda').value='';renderAgenda();toast('Todos os agendamentos exibidos.')});$('btnVerAgendamentosPacienteModal').addEventListener('click',()=>{if(pacienteEditandoId)visualizarAgendamentosPaciente(pacienteEditandoId)});document.querySelectorAll('.home-btn').forEach(btn=>{const pressOn=()=>btn.classList.add('pressing');const pressOff=()=>btn.classList.remove('pressing');['pointerdown','touchstart','mousedown'].forEach(evt=>btn.addEventListener(evt,pressOn,{passive:true}));['pointerup','pointercancel','touchend','touchcancel','mouseup','mouseleave'].forEach(evt=>btn.addEventListener(evt,pressOff,{passive:true}))});$('filtroDataAgenda').addEventListener('input',renderAgenda);$('filtroBuscaAgenda').addEventListener('input',renderAgenda);$('buscaPaciente').addEventListener('input',renderPacientes);$('btnEnviarLembretesAmanha').addEventListener('click',()=>window.abrirLembretesAmanha());$('btnExportarBackup').addEventListener('click',exportBackup);$('inputRestaurarBackup').addEventListener('change',e=>{restoreBackup(e.target.files[0]);e.target.value=''});$('filtroBuscaFicha').addEventListener('input',renderFichas);$('filtroCategoriaFicha').addEventListener('change',renderFichas);$('inputNovaFicha').addEventListener('change',async e=>{await processFichaFiles(Array.from(e.target.files||[]));e.target.value=''});const dropZone=$('uploadDropZone');['dragenter','dragover'].forEach(evt=>dropZone.addEventListener(evt,e=>{e.preventDefault();dropZone.classList.add('dragover')}));['dragleave','drop'].forEach(evt=>dropZone.addEventListener(evt,e=>{e.preventDefault();dropZone.classList.remove('dragover')}));dropZone.addEventListener('drop',async e=>{const files=Array.from(e.dataTransfer?.files||[]);if(files.length)await processFichaFiles(files)});$('pacienteFotoInput').addEventListener('change',e=>{const f=e.target.files[0];if(!f)return;const r=new FileReader();r.onload=x=>{pacienteFotoTemp=x.target.result;updatePhoto()};r.readAsDataURL(f)});$('pacienteArquivosInput').addEventListener('change',async e=>{for(const f of Array.from(e.target.files||[])){const res=await new Promise(ok=>{const r=new FileReader();r.onload=x=>ok(x.target.result);r.readAsDataURL(f)});arquivosPacienteTemp.push({nome:f.name,tamanho:f.size,conteudo:res})}renderArquivosPacienteTemp();e.target.value=''});$('beforeAfterFotoInput').addEventListener('change',async e=>{const f=e.target.files[0];if(!f)return;const res=await new Promise(ok=>{const r=new FileReader();r.onload=x=>ok(x.target.result);r.readAsDataURL(f)});beforeAfterFotoTemp={nome:f.name,conteudo:res}});$('adicionarAntesDepois').addEventListener('click',()=>{if(!beforeAfterFotoTemp)return toast('Selecione uma foto de antes ou depois.');antesDepoisTemp.push({tipo:$('beforeAfterTipo').value,data:$('beforeAfterData').value||todayISO(),avaliacao:$('beforeAfterAvaliacao').value.trim(),foto:beforeAfterFotoTemp.conteudo,nome:beforeAfterFotoTemp.nome});renderAntesDepoisTemp();$('beforeAfterAvaliacao').value='';$('beforeAfterFotoInput').value='';beforeAfterFotoTemp=null;toast('Histórico de antes e depois adicionado.')});$('salvarPaciente').addEventListener('click',()=>{const nome=$('pacienteNome').value.trim();if(!nome)return toast('Informe o nome do paciente.');const payload={nome,telefone:$('pacienteTelefone').value.trim(),cpf:$('pacienteCpf').value.trim(),email:$('pacienteEmail').value.trim(),nascimento:$('pacienteNascimento').value,endereco:$('pacienteEndereco').value.trim(),observacoes:$('pacienteObservacoes').value.trim(),foto:pacienteFotoTemp,arquivos:arquivosPacienteTemp,antesDepois:antesDepoisTemp};if(pacienteEditandoId){const idx=pacientes.findIndex(p=>p.id===pacienteEditandoId);const oldName=pacientes[idx]?.nome;pacientes[idx]={...pacientes[idx],...payload};agendamentos=agendamentos.map(a=>(a.pacienteId===pacienteEditandoId||a.pacienteNome===oldName)?{...a,pacienteId:pacienteEditandoId,pacienteNome:payload.nome,pacienteTelefone:payload.telefone}:a)}else{pacientes.push({id:Date.now(),...payload})}save();fillAgendamentoSelects();closeModal('modalPaciente');clearPacienteModal();renderPacientes();renderAgenda();toast('Paciente salvo com sucesso.')});$('btnExcluirPacienteModal').addEventListener('click',()=>{if(pacienteEditandoId)excluirPaciente(pacienteEditandoId)});$('salvarProcedimento').addEventListener('click',()=>{const nome=$('procedimentoNome').value.trim();const descricao=$('procedimentoDescricao').value.trim();if(!nome||!descricao)return toast('Informe o nome e a descrição do procedimento.');procedimentos.push({id:Date.now(),nome,descricao,valor:Number($('procedimentoValor').value||0)});save();fillAgendamentoSelects();closeModal('modalProcedimento');['procedimentoNome','procedimentoDescricao','procedimentoValor'].forEach(id=>$(id).value='');toast('Procedimento salvo com sucesso.')});$('salvarAgendamento').addEventListener('click',()=>{const pacienteId=Number($('agendamentoPaciente').value);const procedimentoId=Number($('agendamentoProcedimento').value);const data=$('agendamentoData').value;const hora=$('agendamentoHora').value;const profissional=$('agendamentoProfissional').value.trim();const descricaoCurta=$('agendamentoDescricaoCurta').value.trim();const status=$('agendamentoStatus').value;const recorrenciaTipo=$('recorrenciaTipo').value;const recorrenciaQuantidade=Number($('recorrenciaQuantidade').value||1);const paciente=pacientes.find(p=>p.id===pacienteId);const proc=procedimentos.find(p=>p.id===procedimentoId);if(!paciente||!proc||!data||!hora||!profissional)return toast('Preencha paciente, procedimento, data, horário e profissional.');if(agendamentoEditandoId){if(hasConflict(data,hora,agendamentoEditandoId))return toast('Já existe outro agendamento neste horário.');const idx=agendamentos.findIndex(a=>a.id===agendamentoEditandoId);if(idx<0)return toast('Agendamento não encontrado.');const original=agendamentos[idx];agendamentos[idx]={...original,pacienteId,pacienteNome:paciente.nome,pacienteTelefone:paciente.telefone||'',procedimentoId,procedimentoNome:proc.nome,procedimentoDescricao:proc.descricao,procedimentoValor:proc.valor||0,data,hora,profissional,descricaoCurta,status};save();closeModal('modalAgendamento');clearAgendamentoModal();renderAgenda();showAgenda();toast('Agendamento atualizado com sucesso.');return}const datas=recurrenceDates(data,recorrenciaTipo,recorrenciaQuantidade);if(datas.some(dt=>hasConflict(dt,hora)))return toast('Horário já agendado, selecione outra data/horário.');const totalPacote=datas.length;const pacoteId=totalPacote>1?`pacote_${Date.now()}`:null;datas.forEach((dt,i)=>agendamentos.push({id:Date.now()+i,pacienteId,pacienteNome:paciente.nome,pacienteTelefone:paciente.telefone||'',procedimentoId,procedimentoNome:proc.nome,procedimentoDescricao:proc.descricao,procedimentoValor:proc.valor||0,data:dt,hora,profissional,descricaoCurta,recorrencia:recorrenciaTipo,totalPacote,ordemPacote:i+1,pacoteId,status,lembreteWhatsAppEnviadoEm:null}));save();closeModal('modalAgendamento');clearAgendamentoModal();renderAgenda();showAgenda();toast(totalPacote>1?`${totalPacote} sessões agendadas com recorrência.`:'Agendamento salvo com sucesso.')});$('btnExcluirAgendamentoModal').addEventListener('click',()=>{if(agendamentoEditandoId)excluirAgendamento(agendamentoEditandoId)});$('dismissInstallHint').addEventListener('click',()=>{localStorage.setItem(STORAGE_KEYS.installHintDismissed,'1');$('installHint').classList.add('hidden')});$('installHintOk').addEventListener('click',()=>{localStorage.setItem(STORAGE_KEYS.installHintDismissed,'1');$('installHint').classList.add('hidden')})}
function restoreScreen(){const s=localStorage.getItem(STORAGE_KEYS.screen)||'screen-home';if(s==='screen-agenda')showAgenda();else if(s==='screen-pacientes')showPacientes();else if(s==='screen-fichas')showFichas();else showHome();syncDesktopNav(localStorage.getItem(STORAGE_KEYS.screen)||'screen-home')}
function init(){seed();fillAgendamentoSelects();setAppHeight();$('beforeAfterData').value=todayISO();$('filtroDataAgenda').value=todayISO();renderArquivosPacienteTemp();renderAntesDepoisTemp();bind();restoreScreen()}
window.addEventListener('resize',setAppHeight);window.addEventListener('orientationchange',setAppHeight);document.addEventListener('DOMContentLoaded',init);